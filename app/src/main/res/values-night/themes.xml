<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SimpleMemo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/translucentBlack</item>
        <item name="colorOnPrimary">@android:color/background_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <!-- StatusBar -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- NavigationBar -->
        <item name="android:navigationBarColor">@color/almostTransparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

    </style>

    <style name="EditTextColorControl">
        <item name="colorControlNormal">@android:color/darker_gray</item>
        <item name="colorControlActivated">@android:color/background_light</item>
    </style>

    <!-- theme -->
    <color name="background">#333333</color>
    <color name="cardDefault">#444444</color>
    <color name="cardPressed">#666666</color>

    <color name="transparentButtonDefault">#00000000</color>
    <color name="transparentButtonPressed">#33FFFFFF</color>

    <color name="floatingButtonDefault">#555555</color>
    <color name="floatingButtonPressed">#777777</color>


</resources>